/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Simple configuration for Next.js 14
  trailingSlash: false,
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
